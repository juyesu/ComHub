import React from "react";
import Layout from "@/components/layout";
import MainPage from '@/components/MainPage'

const Home = () => {
  return (
    <Layout>
      <MainPage />
    </Layout>
  );
};

export default Home;

